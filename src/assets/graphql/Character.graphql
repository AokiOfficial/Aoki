query ($search: String) {
  Character(search: $search) {
    id
    siteUrl
    name {
      full
    }
    image {
      large
    }
    description
    favourites
    media {
      nodes {
        title {
          romaji
        }
      }
    }
  }
}